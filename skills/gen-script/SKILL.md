---
name: gen-script
description: 改编剧本，从小说原文生成短视频剧本
allowed-tools:
  - read
  - write
  - edit
  - grep
  - glob
triggers:
  - user
  - model
---

# 改编剧本

## 前置条件

- 项目已初始化（有 `config.yaml` 和 `chapters/` 目录）

## 章节目录结构

```
chapters/ch01/
  novel.md      # 小说原文（用户输入或粘贴）
  script.md     # 改编剧本（本工作流生成）
```

## 输入参数

| 参数 | 说明 | 示例 |
|------|------|------|
| **项目路径** | 项目根目录的绝对路径 | `/Users/.../末日求生` |
| **章节号** | 章节编号 | `ch01` |

## 执行步骤

### 1. 收集参数

- 确认项目路径
- 确认章节号（如 `ch01`）

### 2. 检查小说原文

读取 `chapters/<章节号>/novel.md`：

- **有内容**：直接进入步骤 3
- **文件不存在或为空**：提示用户在对话框中直接粘贴小说原文，粘贴后将内容写入 `chapters/<章节号>/novel.md`

### 3. 改编剧本

LLM 将小说原文改编为短视频剧本，写入 `chapters/<章节号>/script.md`：

**剧本格式：**

```
# 第X章 章节名 — 改编剧本

【场景1：场景描述，时间】

角色名（动作/表情）：台词内容。

角色名（动作/表情）：台词内容。

【场景2：场景描述，时间】

（动作描写，无对白）

角色名（动作/表情）：台词内容。
```

**改编原则：**

- **精简对白**：去掉小说中的心理描写和长段叙述，保留核心对话
- **场景化**：明确标注每个场景的切换，用 `【场景X：描述，时间】` 分隔
- **动作可视化**：将小说中的叙述转为可视化的动作描写
- **角色名**：使用剧本中的角色名，后续分镜阶段再通过 character_map.yaml 映射到 assets 目录名
- **无对白段落**：用括号 `（）` 描写纯动作/环境镜头
- **时长控制**：改编后的剧本适合 1-3 分钟的短视频

### 4. 确认结果

- 向用户展示生成的剧本
- 询问用户是否满意，不满意可修改后重新生成
- 确认后提示用户可以执行 `/vc-short:gen-shots` 拆分分镜

## 注意事项

- 剧本要适合短视频节奏，不要太长
- 每个场景要有明确的视觉描述，方便后续分镜
- 角色对白要自然，不要照搬小说原文
- 如果小说中有新角色或新场景，提示用户可能需要先执行 `/vc-short:extract` 提取资产
